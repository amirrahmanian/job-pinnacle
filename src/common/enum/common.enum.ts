export enum Role {
  SuperAdmin = 'Super Admin',
  Admin = 'Admin',
}

export enum Permition {
  Write = 'Write',
  Read = 'Read',
}

export enum ColaborationType {
  PartTime = 'Part Time',
  FullTime = 'Full Time',
  Remote = 'Remote',
}
